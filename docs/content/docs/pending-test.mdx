# 待测试

## Feature 分支能力整合

- 验证创作端直接使用后台公开系统渠道，图片、视频、音频、文本生成不再经过前台逻辑模型路由。
- 验证 Gemini Images 使用独立 `gemini-image` 协议；GlobalAiOpc、汇取云和 AIStarsLab 的图片/视频协议、默认参数与模型目录仍正常工作。AIStarsLab 模型目录已兼容 `defaultOption` 布尔值、可空描述和 `{ min, max, options }` 时长合同。
- 验证 AIStarsLab 同一模型的多条线路各自独立成条：后台拉取后每条线路都有一个 `<线路>:<模型>` 待配置记录，展示名带线路标题可区分，价格、画质、时长、模式和参考素材上限不互相覆盖；不再按模型名去重只保留默认线路。旧的“只存模型名”记录仍在库里，需确认与新记录共存时后台展示和创作端选模型是否歧义，以及是否需要手工清理。
- 验证汇取云 `sd2-mx933-720-*` 与 `sd2-mx933-720-fast-*` 文生视频使用 JSON，带图片、视频或音频参考时改用 multipart 文件上传；首尾帧、9 图/3 视频/3 音频、480P/720P、固定 5/10/15 秒及 `generate_audio` 参数均按官方合同生效，其他汇取云模型保持原协议。
- 验证 AIStarsLab 多图、图片加音频等参考生成不再被错误标记为不存在的 `reference_to_video` 操作；历史任务重试会按真实参考素材还原为 `image_to_video`、`extend` 或 `audio_to_video`。
- 验证 AIStarsLab 创建请求按官方合同上报必填字段：`channel` 取自模型目录线路编码（缺失时明确报错而不是发空值），`quality` 从声明档位选取，`mode` 按参考图数量推导为 `text2video`、`image2video` 或正好 2 张时的 `frames2video`。
- 验证线路块缺失的 AIStarsLab 存量模型（modelKey 形如 `54:seedance-2.5`）无需先去后台“重新拉取模型”即可生成：前后端均按 modelKey 现场还原 `channel` 与官方 `model`，参考素材上限保持未声明而不因默认 0 把参考图卡成超限；不带线路前缀且无线路块时仍明确失败。
- 验证 AIStarsLab 生成请求不再被补上多余的 `/v1`：`/openapi` 被识别为上游 API 根，创建与轮询分别打到 `/openapi/generation/create/{video,image}` 和 `/openapi/generation/status`，不再返回 `No static resource openapi/v1/...`；同时验证普通 OpenAI 兼容渠道仍正常补 `/v1`。
- 验证系统渠道生成时 AIStarsLab 线路块从服务端 `channel_models.capability_config_json` 回填，不再依赖浏览器上报：未定价模型（公开渠道不下发 capabilityConfig）和前端旧缓存场景下，后台重新拉取模型后应能直接生成，不再报“缺少线路编码”。
- 验证 AIStarsLab 任务轮询按官方状态码 1/2/3/4 处理，结果从 `outputs` 数组读取，失败时使用 `errorMessage`；轮询间隔不低于 10 秒以免触发限流。
- 验证 `aistarslab-image` 已有后端生成路径，不再错误地落到通用 OpenAI Images 协议；图生图会上报参考图并受模型 `inputImagesMax` 限制，蒙版编辑明确拒绝。
- 验证 AIStarsLab 参考素材仅接受公网 URL，错误提示不再沿用汇取云文案。官方要求在提示词中用 `@图片1` 这类索引引用素材，当前未自动追加，需确认是否要由产品层补齐。
- 验证支持多个视频分辨率的后台模型可为各分辨率单独配置按次或按秒积分；任务预估、冻结和账单快照按实际分辨率命中，未单独配置的档位使用默认单价。
- 验证积分中心和后台用户管理中的当日、昨日、本周、本月消耗统计会按北京时间读取已结算账单，并显示已有历史消耗。
- 验证后台“资源与策略”可展示并保存自定义渠道频控、并发、请求体、响应体和超时配置，不再因表单漏传字段而提示频控必须为正整数。
- 验证刷新或重新进入画布后可恢复本地运行任务、后端任务和延迟视频轮询，退出画布不会自动取消已提交任务。
- 验证管理员可配置响应拦截与 LibTV，画布可导入 LibTV 资源并正常预览，关闭设置时相关入口和请求明确失败。
- 逻辑模型相关表保留为历史数据兼容结构；新任务不再创建逻辑模型路由记录。

## 画布 Agent 面板拖拽动效

- 验证拖动右侧 Agent 面板宽度时仅保留顶部菜单，消息输出、配置、历史、日志、引用和输入区一起平滑收起。
- 验证松开拖动后内容原位恢复，草稿和消息滚动状态不丢失；系统启用“减少动态效果”时不播放收起和恢复过渡。

## 画布连线交互

- 验证拖动连线靠近普通节点时，临时连线端点与节点侧边可见“+”同步吸附，松开后最终连线保持相同锚点位置。
- 验证单击连线后在线条中间显示删除按钮，点击按钮只删除当前连线且不会误选、拖动画布或节点。

## 视频节点复制与模式判定

- 验证复制或复制粘贴已生成的视频节点时，副本不携带原视频文件、任务、字幕和版本状态，只保留提示词、模型参数与原有参考连线。
- 验证文生视频成功后仍显示文生视频，不会因节点自身已有视频结果被误判为“视频续写”；存在真实上游参考视频时仍正确显示续写模式。

## 画布默认比例

- 验证新画布默认使用全局 `16:9`，可在画布外观面板切换当前画布比例，并支持恢复跟随全局默认。
- 验证“设为全局默认”后其他未单独设置比例的画布同步更新，已覆盖比例的画布保持自身设置。
- 验证节点单独设置的比例优先于当前画布比例，图片、视频、批量生成和失败重试均使用相同覆盖关系。

## 画布后台任务持久化

- 验证生成任务运行时退出或切换画布只停止当前页面观察，不再向后端发送取消；任务继续执行并持久化结果。
- 验证重新进入画布后可恢复排队、运行和已完成任务，用户点击“停止生成”时仍会明确取消后端任务。

## 后端数据库日志

- 验证系统设置尚未保存时仍正常使用默认配置，但后端不再重复输出预期的 GORM `record not found` 日志；真实 SQL 错误仍会输出。

## 1Panel 镜像部署

- 验证 `POSTGRES_PASSWORD` 留空时，PostgreSQL 容器启动脚本会生成 48 位字母数字密码，后端与 Web 正常启动，编排不再依赖独立的 `alpine:3.22` 服务。
- 验证手动填写少于 32 位或包含非字母数字字符的 `POSTGRES_PASSWORD` 时，PostgreSQL 容器明确失败。
- 验证重新创建编排时继续复用 `deployment-secrets` 数据卷中的密码，不覆盖现有数据库和登录账号。
- 验证 1Panel 未导入环境变量时，编排默认使用 `http://192.204.35.56:6868` 作为 CORS Origin 并成功创建。
- 验证服务器通过 `0.0.0.0:6868` 暴露 Web，并可从 `http://192.204.35.56:6868` 正常访问和调用同源 API。

## GlobalAiOpc 独立渠道预设

- 验证新增 GlobalAiOpc 渠道后固定使用 `https://zcbservice.aizfw.cn/kyyReactApiServer`，并仅为该渠道预置 `globalaiopc-image` 与 `globalaiopc-video` 模型协议。
- 验证 OpenAI、Gemini 和普通自定义渠道不显示 GlobalAiOpc 专属协议，已有模型、Base URL、默认参数和调用行为保持不变。
- 验证 GlobalAiOpc 图片模型按模型限制分辨率，视频模型按能力表限制时长、比例、分辨率、声音和水印；Seedance 1.5 的两张参考图分别作为首帧与尾帧。
- 验证 `seedance-2.5-c2` 仅允许参考图片和参考音频，时长限制为 4–29 秒，并正确识别数字或字符串形式的上游错误码。
- 验证浏览器和后端任务执行器均通过登录态 `/api/ai/custom` 中转，且仅 `X-Canvas-Upstream-Format: globalaiopc` 放行 `/v2/model-center/tasks`。
- 当前未配置真实 GlobalAiOpc API Key，仅完成本地构建和接口边界检查；真实图片、视频生成及任务轮询仍需使用有效密钥验证。

## 火山方舟独立渠道预设
- 验证火山方舟 Seedance 2.0/2.5 私域虚拟人素材使用 `asset://<asset_ID>`：任务中转、直连和后端请求体原样保留该 URI，普通渠道不放行该协议；素材上传、Active 状态轮询、ProjectName 与 AK/SK 管理仍需按火山 Assets API 另行配置。

## 汇取云独立渠道预设

- 验证新增汇取云渠道后固定使用 `https://api.bjhuiqu.net/v1`，并且只有该渠道按模型名自动识别文本、图片、视频和音频协议。
- 验证管理员“系统渠道 → 新增系统渠道”可直接选择“汇取云”，固定 Base URL 后拉取模型会自动补全能力、协议与图片/视频能力参数；已有汇取云系统模型重新拉取时同步纠正合同，但保留价格和启用状态。
- 验证汇取云模型完成定价配置后，可由管理员纠正错误匹配的能力与协议；再次拉取模型不会覆盖人工确认的合同。
- 验证 `sd2-mx933-720-*` 与以 `-5s`、`-10s`、`-15s` 结尾的模型名不再被误判为文本模型；这类模型即使已定价且早期被存成 `chat-completion`，重新拉取模型时也会被纠正为汇取云视频协议，不再返回“无效请求”。
- 验证汇取云 `mj-sd2.0-933-720p` 及其 `-5s`、`-10s`、`-15s`、`-fast` 变体统一使用汇取云视频协议和 933 专属能力；启动或重新拉取时会修复历史 `newapi-channel-2` 合同，保留价格与启用状态。
- 验证 OpenAI、Gemini、GlobalAiOpc 与普通自定义渠道的协议列表、模型能力、Base URL 和调用行为保持不变。
- 验证汇取云视频创建使用 `/videos/generations`，轮询使用 `/videos/{id}`，缺少结果 URL 时从 `/videos/{id}/content` 下载成品。
- 验证汇取云视频限制为 4–15 秒、720P 和声明的六种比例；模型名以 `-5s`、`-10s`、`-15s` 结尾时固定对应时长。
- 验证最多 4 张参考图、3 个参考视频和 1 个参考音频；多图参考遵循模型声明的 4–15 秒与比例选项，不再强制 8 秒、16:9；音频参考必须同时提供图片或视频。
- 当前未配置真实汇取云 API Key；真实模型列表、文本、图片、音频、视频创建与轮询仍需使用有效密钥验证。

## NewAPI Video Generations 参考音频

- 验证参考音频必须同时提供至少 1 张参考图片，不要求参考视频；旧画布仍标记为 `text_to_video` 时也会把已连接图片写入 `image_urls`。
- 验证纯音频以及仅有参考视频和参考音频的请求均被明确拒绝，不再提示“必须提供参考视频”。

## 用户自定义渠道下线

- 验证用户设置不再显示“自定义渠道”，访问旧的 `?section=channels` 链接会回到“模型选择”。
- 验证创作端模型列表只使用后台“系统渠道”中已启用且已定价的模型，浏览器历史自定义渠道和本机渠道配置不会参与生成。
- 验证后台“用户功能开放”和“运行策略”不再显示自定义渠道开关及中转参数。
- 验证 `/api/ai/models`、`/api/ai/custom` 和带自定义渠道配置的新建或重试任务均明确拒绝，已有自定义渠道历史配置不会被删除。

## 前台模型下线

- 验证登录会话不再返回或注入“前台模型/平台模型”目录，创作端只读取后台已启用的系统渠道与模型。
- 验证管理后台不再显示“前台模型”菜单，访问旧 `/admin/models` 地址会跳转到“系统渠道”。
- 验证旧浏览器配置中的 `managed` 平台模型缓存会在恢复配置时自动清理，不会重新出现在模型选择器。
- 验证新建生成任务、任务页操作和画布分镜任务不再提交 `logicalModelId`，历史逻辑模型表和旧任务数据仍保留用于读取。

## 团队共享素材库

- 验证个人文本、图片、视频、音频和 3D 模型可共享到团队素材库，浏览器临时媒体会先上传到后端资源存储。
- 验证其他登录账号可搜索、预览并从画布素材选择器插入团队文本、图片、视频和音频。
- 验证上传者和管理员可重命名、取消共享，普通成员不能修改他人共享素材。
- 验证素材库将“团队素材”作为独立一级列表展示，个人和团队文件夹互不混用。
- 验证个人与团队素材卡片都支持右键菜单和显式更多按钮；团队成员可复制文本或下载媒体，上传者和管理员可移动素材。
- 验证个人素材共享到团队时可先选择团队根目录或具体团队文件夹，确认后素材直接进入所选目录。
- 验证个人与团队文件夹可新建、重命名、删除；删除文件夹只把素材移回根目录。
- 验证取消共享不会删除上传者的个人素材和资源文件，且共享后私有资源不会匿名公开。
- 验证 SQLite 与 PostgreSQL 启动迁移会创建 `team_assets`、`team_asset_folders` 和 `team_asset_resources`。

## 3D 导演台骨骼

- 验证平移、旋转和缩放人物时，骨骼辅助线、骨骼控制点与人物模型保持同步，不出现重复位移或偏离。
- 验证演员首次加载时直接应用站立姿态，骨骼辅助线与控制点首帧位置正确，切换姿势和骨骼关键帧后仍正常更新。

## 提示词代码编辑器依赖收敛

- 验证提示词代码编辑器仍能正常输入、换行折行、显示行号并按明暗主题切换配色。
- 验证通过编辑器句柄插入文本和聚焦（技能/变量插入路径）行为不变。
- 背景：`EditorView` 改为从 `@uiw/react-codemirror` 再导出入口导入，消除 `@codemirror/view` 双版本类型冲突导致的 `tsc --noEmit` 失败。

## 七牛对象存储无绑定域名

- 验证七牛 Kodo 不填 CDN 加速域名也能保存设置，后台与个人存储表单不再强制该字段。
- 验证未绑定域名时资源访问走后端代理（前端不再拼 `?direct=1`），图片、视频预览和下载正常。
- 验证已绑定域名的私有空间返回带签名的重定向直链，过期时间内可正常读取。
- 验证无域名场景下的 S3 兼容签名（`{bucket}.s3.{region}.qiniucs.com`）在 z0/z1/z2/na0/as0/cn-east-2 各 region 都能正确寻址。

## 创作对话文本持久化与回放

- 验证创作页文本生成时后端会建一个 `text_replay` 存档任务：不排入队列、不占 active 任务额度、不产生计费。
- 验证生成过程中正文按 2048 字符阈值节流上报，生成完成后任务收尾为 succeeded 并写入最终正文。
- 验证生成完成后刷新页面，消息从后端终态恢复出完整正文，不再停在“正在生成…”。
- 验证生成中途关闭浏览器后重新进入：任务停在 `text_replay`，页面应交还已存的部分正文并提示生成被中断，**不得报“生成失败”**。
- 验证生成进行中的当前标签页不会被恢复链路干扰：流式写入的正文不会被误判为中断而覆盖。
- 验证生成失败或用户主动停止时消息直接进入终态，刷新后不会被恢复逻辑二次改写。
- 待确认：用户中途停止生成时，后端 `text_replay` 任务不会被收尾，会以该状态遗留（回放增量 7 天后过期清理，任务记录保留）；是否需要为这类任务补一条显式收尾路径待定。

## 工作台侧栏改造与技能库分页

- 验证侧栏换成新的 `WorkspaceSidebarNav` 后 8 个导航项（创作、项目、画布、任务、素材、技能、钱包、设置）齐全且选中态正确；管理员账号额外出现后台入口，普通账号不出现。
- 验证原 `workspace-sidebar-footer` 的四项能力都有承接：后台入口在侧栏导航、退出登录在侧栏 footer、深色模式切换与登录入口在新的账户菜单。
- 验证退出登录后会话被清空且跳回登录页；`use-workspace-logout` 已按本地 `AuthSessionPayload` 使用 `systemChannels`，需确认退出后重新登录时系统渠道正常拉取（此前修复汇取云的那处改动不得回退成 `logicalModels`）。
- 验证命令面板可打开、可搜索、可跳转，且路由预加载不报错。
- 验证画布页换用 `CanvasFolderCard` 后重命名、多选、批量删除、预览封面（优先取最新图片）都正常。
- 验证技能库页新的 hero 区与分类 tabs 展示正常，分页控件可翻页。
- **重点回归**：`PaginationBar` 由 AntD `Pagination` 换成自研实现，被 16 个页面共用（含 9 个后台页），需逐页确认分页控件样式与翻页行为；尤其验证页码较多出现两个省略号时不再有 React key 重复告警。
- 验证浮层（下拉、气泡、弹窗、抽屉、通知）背景改为不透明且取消阴影后，明暗主题下层级仍可分辨；气泡卡片覆盖已按 antd 6 语义改用 `.ant-popover-container`。
- 验证创作页空态推荐卡图标（`suggest-icon`）与钱包页余额图标（`wallet-balance-icon`）改用共享 `library-icon-tile` 后仍居中且有浅底。
- 待确认：`--library-icon-surface` 这个 token 在仓库里从未定义（main 与 upstream 都缺），`library-icon-tile` 的浅底实际不生效，图标底色为透明。是否补 token 待定。

## AIStarsLab 画质档位错位（选 720 实发 480）

- **必须先在后台重新拉取 AIStarsLab 渠道模型**，让线路声明的画质档位写进能力配置；否则前端仍会展示按模型名猜的通用档位（480p~2160p）。
- 验证重新拉取后创作页与画布的分辨率选项只剩该线路真实支持的档位，不再出现选了也用不了的档位。
- 验证选 720 实际向上游发出 720p，选 1080 发出 1080p；不再出现“选什么都按首档出图、却按所选档位计费”。
- 验证各档位单独定价后，任务预估、冻结和账单快照命中的是实际出图档位的单价。
- 验证后台“分辨率价格”只能填该线路声明的档位，填入不支持的档位仍明确报错。
- 验证画质真的对不上时（例如前端旧缓存仍存着旧档位）生成明确失败并提示可选档位，不静默降级。
- 验证图片模型的 1K/2K 类非分辨率档位仍按原串上报，auto 仍回退到首档。
