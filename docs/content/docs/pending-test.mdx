# 待测试

## 后端数据库日志

- 验证系统设置尚未保存时仍正常使用默认配置，但后端不再重复输出预期的 GORM `record not found` 日志；真实 SQL 错误仍会输出。

## 1Panel 镜像部署

- 验证 `POSTGRES_PASSWORD` 留空时，PostgreSQL 容器启动脚本会生成 48 位字母数字密码，后端与 Web 正常启动，编排不再依赖独立的 `alpine:3.22` 服务。
- 验证手动填写少于 32 位或包含非字母数字字符的 `POSTGRES_PASSWORD` 时，PostgreSQL 容器明确失败。
- 验证重新创建编排时继续复用 `deployment-secrets` 数据卷中的密码，不覆盖现有数据库和登录账号。
- 验证 1Panel 未导入环境变量时，编排默认使用 `http://192.204.35.56:6868` 作为 CORS Origin 并成功创建。
- 验证服务器通过 `0.0.0.0:6868` 暴露 Web，并可从 `http://192.204.35.56:6868` 正常访问和调用同源 API。

## GlobalAiOpc 独立渠道预设

- 验证新增 GlobalAiOpc 渠道后固定使用 `https://zcbservice.aizfw.cn/kyyReactApiServer`，并仅为该渠道预置 `globalaiopc-image` 与 `globalaiopc-video` 模型协议。
- 验证 OpenAI、Gemini 和普通自定义渠道不显示 GlobalAiOpc 专属协议，已有模型、Base URL、默认参数和调用行为保持不变。
- 验证 GlobalAiOpc 图片模型按模型限制分辨率，视频模型按能力表限制时长、比例、分辨率、声音和水印；Seedance 1.5 的两张参考图分别作为首帧与尾帧。
- 验证 `seedance-2.5-c2` 仅允许参考图片和参考音频，时长限制为 4–29 秒，并正确识别数字或字符串形式的上游错误码。
- 验证浏览器和后端任务执行器均通过登录态 `/api/ai/custom` 中转，且仅 `X-Canvas-Upstream-Format: globalaiopc` 放行 `/v2/model-center/tasks`。
- 当前未配置真实 GlobalAiOpc API Key，仅完成本地构建和接口边界检查；真实图片、视频生成及任务轮询仍需使用有效密钥验证。

## 团队共享素材库

- 验证个人文本、图片、视频、音频和 3D 模型可共享到团队素材库，浏览器临时媒体会先上传到后端资源存储。
- 验证其他登录账号可搜索、预览并从画布素材选择器插入团队文本、图片、视频和音频。
- 验证上传者和管理员可重命名、取消共享，普通成员不能修改他人共享素材。
- 验证素材库将“团队素材”作为独立一级列表展示，个人和团队文件夹互不混用。
- 验证个人与团队素材卡片都支持右键菜单和显式更多按钮；团队成员可复制文本或下载媒体，上传者和管理员可移动素材。
- 验证个人素材共享到团队时可先选择团队根目录或具体团队文件夹，确认后素材直接进入所选目录。
- 验证个人与团队文件夹可新建、重命名、删除；删除文件夹只把素材移回根目录。
- 验证取消共享不会删除上传者的个人素材和资源文件，且共享后私有资源不会匿名公开。
- 验证 SQLite 与 PostgreSQL 启动迁移会创建 `team_assets`、`team_asset_folders` 和 `team_asset_resources`。
