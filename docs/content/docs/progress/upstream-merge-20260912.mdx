---
title: v1.2.9 合并结果（2026-09-12）
description: 上游 17 个提交的接入结果、本地增强保留理由、冲突处理与验证范围
---

# v1.2.9 合并结果

本次目标为上游 `01a2d171f5b2a6854a987ba2022a8d960d9107b4`（`v1.2.9`），基线为 `0945115e531962eb44b27272a8e8d9ea40661f65`（`v1.2.8.rc3`）。两者之间的 17 个上游提交全部进入本次合并，没有整批拒绝的上游功能。

合并前先保存暂存区、未暂存修改、全部 9 个新增文件和 Git 合并状态；此前 rc3 合并及本地增强已收尾为本地检查点 `9452c09`。本次不推送远端，不发布镜像，不启动数据库迁移，不修改真实账号或业务账目。

常规 Git fetch 在当前网络失败，改用系统 HTTPS 调用同一 GitHub 仓库的标准 `git-upload-pack`，随后通过 `git index-pack --strict` 导入并校验对象，确认 rc3 是目标提交的祖先。没有将 ZIP 快照伪装成上游提交，也没有改写上游历史。

## 一、已接入的上游内容

- 接受 `app`、`auth`、`platform`、`kernel` 等后端领域拆分；`service` 保持上游的稳定导入面，不塞回本地业务实现。
- 接受图片/音频声明式协议迁移及二进制媒体响应支持，保留上游新的插件目录。
- 接入全景图节点、配置、环视、截图、四向导出及分享降级。
- 接入打光和镜头工具流程优化、创作模式切换、助手 AI 问答卡片恢复。
- 接入图片多级缓存和直链首帧优化、welcome 路由与镜像资源修复、图片复制修复。
- 接入显式文本引用、拖动连线替换参考图、引用同步与画布保存冲突修复。
- 接入方舟可信素材重试、素材 ID、人像认证标记和支付订单用户身份详情。
- 接入上游发布、文档及相邻测试更新。

## 二、没有直接覆盖的内容与原因

下列项目已按上游结构保留，不是冲突残留，也不是拒绝上游整个模块。

| 项目 | 处理结果 | 不直接覆盖的原因 |
| --- | --- | --- |
| 管理统计和个人明细 | 迁入 `app/analytics.go`，保留仓储、接口、前端面板和测试 | 上游没有同等的个人积分、秒数、渠道/模型/每日明细及结算口径 |
| 积分中心周期消耗 | 迁入 `app/finance.go`，保留个人钱包接口及页面 | 上游钱包不包含今日、昨日、本周、本月及每日消耗聚合 |
| 用户名修改 | 保留 `app/admin.go` 与 `repository/admin_user.go` | 上游更新请求没有用户名字段；不能丢失用户已要求的改名和归属保护 |
| 独立密码重置 | 保留弹窗、随机密码、事务更新、旧会话撤销与脱敏审计 | 上游基础改密不足以替代本地已完成的管理操作流程 |
| 存储恢复和显式凭据读取 | 迁入 `app/settings.go`、`storage_location.go` | 防止恢复备份后因旧密钥丢失再次无法配置；保留管理员权限、审计与禁止缓存边界 |
| 任务快照防回退 | 保留共享订阅保护和中断重连 | 与上游画布保存冲突修复不是同一链路，两者都需要 |
| 提示词高度及松手吸附 | 保留 16 行上限和线头跟手；接入上游参考图替换 | 两类连线行为共存：先判断参考图替换，再按普通卡片/端口规则提交连接 |
| 特定渠道适配 | 本地请求准备和测试迁入 `app`，协议注册留在 `protocol` | GlobalAiOpc、AIStarsLab、汇取云、微进仍需要特定参数、素材和结果处理 |
| 多 Provider 宿主清单 | 保留受信任内置源的 `host:providers` 解析 | 部分本地渠道包同时提供多种能力；不向任意第三方插件放开宿主执行 |
| 旧数据库迁移谱系 | 保留精确连续 checksum 桥接及测试 | 恢复库来自旧稳定分支，不能用忽略校验代替正确谱系识别 |
| Windows 指标 | 移至 `app/system_metrics_windows.go`，保留 build tag | 本地开发需要指标实现，且不能和通用 fallback 重复编译 |

## 三、旧差异已收敛的项目

### AutoDL 插件

对二进制安装包内部逐项比较：本地与 v1.2.9 的 README、接口文档一致，清单的所有非 documentation 字段一致，差别仅为嵌入文档内容。因此采用上游安装包，没有丢失 ZM U24/U08 等工作流和参数，不再保留这一重复差异。

### 牌谱插件

保留本地牌谱插件，但升级为 `yingce.plugin/v2`，版本 `1.0.1`，按上游自包含插件要求增加参数说明、接口文档和自动生成的完整清单合同。新增 `build.ts` 可重新打包。

请求地址及既有映射保留，新增测试覆盖文本额外参数、Agent、图片、视频、轮询和结果提取。没有借此次合并改服务商协议，也未发送计费请求。原有首张图片结果映射仍在，不能宣称批量图片结果已经全部验证。

安装包：`plugin-packages/paipu-net.yingce-plugin`。

SHA-256：`AEBC9DE5D7AE52C8A0FE33E924087F7FF208E1023595D630108ED797D18B269A`。

### 不恢复的旧实现

NewAPI Channel 2 旧适配、独立的旧画布远程同步文件、依赖旧结构的整套稳定性测试继续不恢复，原因仍是上游已有等价实现或测试已按现有结构移植。不得为了让文件名看起来齐全而撤销上游分层。

## 四、本次冲突和验证修正

- 用户管理导入冲突：保留上游 `kernel` 工具引用，保留本地原子用户更新，移除已不用的导入。
- 协议注册冲突：保留本地渠道注册，同时接受上游图片/音频协议迁移，不重新注册已退役的宿主适配器。
- 资源 payload 测试：保留更完整的 ID、封面、标签、时间戳断言。
- 待测试文档：两边内容均保留，并新增本次结果与待验收项。
- 本地新增后端文件及测试迁到 `app`；认证错误引用改用 `kernel`；密码测试直接核验 bcrypt，不新增生产兼容接口。
- Windows 权限测试只在非 Windows 平台断言 POSIX 权限，配置内容和保存行为仍验证。
- 图片弹窗测试跟随当前 `AppModal` 外壳和默认销毁行为；视频首帧静态测试兼容 CRLF，不改生产行为。
- 资源删除测试使用单个 SQLite 连接并清理连接池，避免异步删除 worker 与断言竞争造成测试锁失败；未改生产资源删除逻辑。

## 五、验证结果

| 验证 | 结果 |
| --- | --- |
| 后端 `go test ./... -count=1` | 通过，1,174 个通过事件，9 个环境条件跳过，无失败 |
| PostgreSQL 并发改名 | 在独立临时 PostgreSQL 17 容器内通过；使用临时内存数据目录，结束后清理容器 |
| 前端专项 | 17 个文件、181 项通过；覆盖统计展示、个人钱包、账号密码、存储、任务快照、画布持久化、引用、尺寸和连线共存 |
| 前端类型 | `tsc --noEmit` 通过；完整构建中再次通过 |
| 前端生产构建 | `bun run build` 通过；仍有上游已有的大 chunk 提示，不是构建失败 |
| 后端可执行文件 | `go build ./cmd/server` 通过，产物在忽略目录，未替换运行实例 |
| 牌谱与官方包目录校验 | v2、自包含文档和完整合同、参数/响应专项均通过 |

全量后端中跳过的项目包括：未配置 DSN 的 PostgreSQL 资产 ID 迁移、缺少本机 redis-server 的两项集成、Windows 不适用的五项 POSIX 支付进程测试、未配置 DSN 的改名并发测试。最后一项已通过上述独立容器补验。ffmpeg 不在当前路径，视频测试中的 MPEG-4 终态分支按既有逻辑略过。

测试和构建不等于浏览器及真实上游验收。本轮没有改真实密码、读取存储密钥、触发计费生成、迁移业务库或覆盖备份。

## 六、明确未做的事

1. 未更改统计日期口径：后台仍为 UTC，个人积分中心仍为 Asia/Shanghai。
2. 未将积分/秒改为人民币成本，未重做跨日任务与结算账单的逐单对齐，也未重算余额。
3. 未实测全景 WebGL、手机布局、明暗主题、真实渠道生成、支付进程、Redis 和 ffmpeg 的目标环境行为。
4. 未替换当前已运行的后端进程。前端开发服务会热读源码，浏览器已有页面可能需要刷新；完整联调应在明确重启新后端后进行。
5. 未推送或发布镜像，生产环境不会因本地合并自动升级。

这些是独立的验收或需求工作，不是漏合并。后续记录应以本文和实际 Git 祖先关系为准，合并前差异报告仅保留为历史依据。
